name: AI BDD & Tests

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  ai-pipeline:
    runs-on: ubuntu-latest

    services:
      ollama:
        image: ollama/ollama:latest
        ports:
          - 11434:11434
        options: >-
          --ipc=host
          --gpus all

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Esperar o Ollama subir
        run: |
          sleep 10
          curl http://localhost:11434/api/tags || exit 1

      - name: Gerar BDDs
        run: |
          python3 ai/generate_bdd.py requirements/criterios.md

      - name: Gerar testes
        run: |
          python3 ai/generate_tests.py

      # - name: Clone do repositório
      #   run: |
      #     git clone https://github.com/Felipe-Janazi/integration-agent.git
      
      # - name: Entrando no repositório
      #   run: |
      #     cd integration-agent
      
      # - name: Configuração playwright
      #   run: |
      #     npm init -y && npm i -D @playwright/test typescript ts-node && npx playwright install --with-deps
      
      # - name: Instalação Ollama 
      #   run: |
      #     curl -fsSL https://ollama.com/install.sh | sh
      
      # - name: Rodando os testes 
      #   run: |
      #     npx playwright test
      #   continue-on-error: True

